[project]
name = "market-scout"
version = "0.1.0"
description = "A minimal implementation of the market-scout validation loop."
requires-python = ">=3.8"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
